#include <stdio.h>
int main()
{
    int a = 3;
    int b = 5;
    int c = 7;
    int output = a*b+c;
    printf("this is simpal math : %d * %d + %d", a,b,c);
    printf("output: %d",output);
    return 0;
}